import type { Theme } from '@emotion/react'

export const defaultTheme: Partial<Theme> = {
  colors: {
    primary: '#4F46E5', // Primary color (Indigo 600)
    background: '#FFFFFF', // Background color (White)
    secondary: '#E0E7FF', // Secondary color (Light Indigo)
    text: '#1F2937', // Text color (Dark Gray)
  },
}
