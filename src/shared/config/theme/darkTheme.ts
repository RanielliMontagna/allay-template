import type { Theme } from '@emotion/react'

export const darkTheme: Partial<Theme> = {
  colors: {
    primary: '#818CF8', // Primary color (Indigo 400)
    background: '#1F2937', // Background color (Dark Gray)
    secondary: '#374151', // Secondary color (Gray 700)
    text: '#F9FAFB', // Text color (Light Gray)
  },
}
